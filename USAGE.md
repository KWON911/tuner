# 사용법

## 시작하기

1. 페이지 하단의 **▶ 마이크 시작** 버튼을 누른다.
2. 브라우저가 마이크 권한을 물으면 허용한다.
3. 버튼이 **■ 중지**로 바뀌고 상태 문구가 "실시간 음정 감지 중..."으로 바뀌면 준비 완료다.
4. 악기를 연주하거나 소리를 내면 화면이 실시간으로 반응한다.
5. 다시 누르면 마이크가 꺼지고 화면이 초기 상태로 돌아간다.

마이크 권한을 거부했거나 마이크 장치가 없으면 상태 문구에 그 이유가 표시된다.

## 음이름 패널

화면 맨 위 패널에 세 가지 값이 표시된다.

| 표시 | 의미 |
|---|---|
| 큰 글자 (예: `F#`) | 검출된 음이름 |
| 그 아래 숫자 (예: `6`) | 옥타브 번호 |
| 맨 아래 (예: `1505.6 Hz`) | 실제 주파수 |

옥타브 번호는 과학적 음높이 표기법을 따른다. 피아노 한가운데 도가 C4이고,
기준음 라(A)는 A4다. 위로 갈수록 옥타브 숫자가 커지고, 아래로 갈수록 작아진다.

소리가 없거나 음을 잡지 못하면 세 값 모두 `—`로 표시된다. 이때 화면이
바로 초기화되지 않고 약 0.5초 동안 마지막 값을 유지하는데, 연주 중 음이
잠깐 끊길 때마다 화면이 깜빡이는 것을 막기 위해서다.

## 튜닝 미터

음이름 패널 아래 막대는 검출된 음이 기준음에서 얼마나 벗어났는지(센트, ¢)를
보여준다. 반음의 100분의 1이 1센트다.

- **바늘이 가운데(초록 구간)에 있고 글자가 초록색** — 정확한 음정 (±5¢ 이내)
- **바늘이 왼쪽, 글자가 노란색, ♭ 표시** — 음이 낮다(flat)
- **바늘이 오른쪽, 글자가 빨간색, ♯ 표시** — 음이 높다(sharp)

미터 아래 숫자(예: `+30 ¢`)가 정확한 센트 편차다.

## 입력 레벨

그 아래 막대는 마이크로 들어오는 소리의 크기를 실시간으로 보여준다.
음정 검출 여부와 무관하게 소리가 있으면 항상 움직인다 — 이 막대가 안 움직이면
마이크 자체가 소리를 못 받고 있다는 뜻이고, 이 막대는 움직이는데 음이름이
안 나오면 소리는 들어오지만 음정을 판별하기 어려운 상태(잡음이 많거나
너무 작은 소리)라는 뜻이다.

## 튜닝 선택

**튜닝** 드롭다운에서 크로매틱(모든 반음) 또는 악기별 표준 튜닝을 고를 수 있다.

| 튜닝 | 줄 (낮은 음 → 높은 음) |
|---|---|
| 크로매틱 | C C# D D# E F F# G G# A A# B (12개 반음) |
| 기타 | E2 A2 D3 G3 B3 E4 |
| 베이스 | E1 A1 D2 G2 |
| 우쿨렐레 | G4 C4 E4 A4 |
| 바이올린 | G3 D4 A4 E5 |

악기를 선택하면 화면 아래쪽 키보드 자리에 12개 반음 대신 그 악기의 줄
목록이 나타난다. 소리를 내면 검출된 음에 가장 가까운 줄이 강조 표시된다.

## 기준음(A4) 보정

기본값은 440Hz다. 슬라이더로 415~466Hz 사이에서 조절할 수 있다. 오케스트라마다
기준음이 조금씩 다르거나(예: 442Hz), 옛 악기·특정 장르에 맞춰 낮은 기준음
(예: 415Hz, 바로크 피치)을 쓸 때 사용한다.

값을 바꾸면 옆의 숫자가 노란색으로 바뀌어 기본값에서 조정되었음을 표시한다.
**440** 버튼을 누르면 기본값으로 즉시 되돌아간다.

기준음을 바꾸면 같은 소리라도 센트 값과 판정이 달라지고, 아래 키/줄을 눌러
재생하는 기준음도 바뀐 기준을 따른다.

## 기준음 재생

12키(크로매틱) 또는 악기 줄 목록의 버튼을 누르면 해당 음을 2초간 스피커로
들려준다. 목표 음이 어떤 높이인지 귀로 먼저 확인하고 싶을 때 쓴다.

**주의:** 마이크를 켠 상태에서 스피커로 기준음을 재생하면 그 소리가 다시
마이크로 들어가 화면이 잠깐 흔들릴 수 있다. 가능하면 헤드폰을 쓰거나,
기준음을 들을 때는 마이크를 잠시 꺼두는 것을 권한다.

## 문제 해결

| 증상 | 확인할 것 |
|---|---|
| 마이크 시작이 안 됨 | 브라우저 주소창의 마이크 권한 설정 확인 |
| 입력 레벨 바가 안 움직임 | 마이크 장치 선택이 올바른지, 음소거 상태가 아닌지 확인 |
| 입력 레벨은 움직이는데 음이름이 안 나옴 | 소리를 더 크고 또렷하게 내보거나, 주변 소음을 줄여본다 |
| 기준음 소리가 안 들림 | 기기 음량을 확인. 저음(기타/베이스 낮은 줄)은 작은 스피커에서 원래 작게 들린다 |
| `file://`로 열었을 때 아무 것도 안 됨 | 더블클릭으로 열 수 없다. `npm run serve` 실행 후 안내되는 주소로 접속 |
